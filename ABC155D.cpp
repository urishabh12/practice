#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main()
{
    ll n, k;
    cin >> n >> k;
    vector<ll> v(n);
    int neg = 0, zero = 0;
    for (auto &a : v)
        cin >> a;
    sort(v.begin(), v.end());
}
